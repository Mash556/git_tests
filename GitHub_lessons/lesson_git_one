git init -создает репозитории скрытый называется .git
git add . - отслеживает данную папку называют это индексом и там можно указать имя файла или папки 
git commit -m "тут мы оставляем комментарии"  - это команда добавляет то что хранится в индексе но оно вот так и не закончится
git push origin name - это команда закидывает все изменение в репозиторию и оно пользуется после этих двух верхних команд
git status - показывает что унас отслеживается и что нет 
git branch - показывает на какой ветке мы находимся
git branch name - создает новую ветку
git checkout name - переходит по указонному ветку
git clone git@github.com:Mash556/git_tests.git - cоединяет удаленную репозиторию с локальным а точнее кланирует
git remote add origin {ssh|https} - создает связь между локальным и удаленным репозиторием

